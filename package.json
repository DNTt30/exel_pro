{
  "name": "exel-pro-db",
  "private": true,
  "description": "Prisma (server-side only) — không import vào frontend Vite",
  "scripts": {
    "prisma:generate": "prisma generate",
    "prisma:pull": "prisma db pull",
    "prisma:migrate": "prisma migrate dev",
    "prisma:studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^6.16.2"
  },
  "devDependencies": {
    "prisma": "^6.16.2"
  }
}
